services:
  kneejoy:
    build: .
    ports:
      - "8080:8080"
    environment:
      NODE_ENV: production
      API_PORT: 8080
    volumes:
      - kneejoy-data:/app/server/data
    restart: unless-stopped

volumes:
  kneejoy-data:
