# Render 一键部署（免费层）
# 在 Render Dashboard → New → Blueprint 导入本仓库即可
services:
  - type: web
    name: kneejoy
    runtime: docker
    plan: free
    healthCheckPath: /api/v1/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: API_PORT
        value: "8080"
